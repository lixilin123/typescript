/**
  * 练习对象：声明文件
  */

{
	// 在声明文件中的声明语句
	// declare var jquery: (string) => any;

	// 用三斜线指令表示引用了声明文件
	/// <reference path="./jquery.d.ts" />

	// 可以直接用npm安装对应的声明模块即可
	// npm install @types/jquery --save-dev
	
}

/**
 * ============ 注意点 ============
 * 1.我们约定声明文件以 .d.ts 为后缀；
 */
